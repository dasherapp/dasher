type User @model {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  githubUserId: String! @unique
  name: String
  login: String! @unique
  avatarUrl: String!
  boards: [Board!]!
}

type Board @model {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
  owner: User!
  columns: [Column!]!
}

type Column @model {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
  board: Board!
  query: String!
  index: Int!
}
